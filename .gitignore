node_modules/
dist/
*.log
.env
.env.*
*.db
*.sqlite
*.sqlite3
.DS_Store

# Astro build cache
.astro/

# Vercel build output — must NOT be committed. If this dir is in the repo,
# Vercel skips the real build and deploys stale function bundles → 500s
# (ERR_MODULE_NOT_FOUND on all SSR routes + /api/*).
.vercel/

# Local secrets (never commit)
pumkin-secrets.txt
.env
.env.local

# SQLite sidecar files (WAL/SHM/journal)
*.db-*

# keep the example env template tracked
!.env.example
