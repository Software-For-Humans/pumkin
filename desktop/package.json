{
  "name": "agentkit-desktop",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build:web": "cross-env NODE_ENV=production npm --prefix ../web run build && node ./scripts/copy-static.mjs",
    "tauri": "tauri",
    "dev": "npm run build:web && tauri dev",
    "build": "npm run build:web && tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "cross-env": "^7.0.3"
  }
}
